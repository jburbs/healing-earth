# Netlify configuration for Healing Earth with Technology
# Static site - no build step needed

[build]
  publish = "."

  [[headers]]
    for = "/*"
      [headers.values]
          X-Frame-Options = "DENY"
              X-Content-Type-Options = "nosniff"
                  Referrer-Policy = "strict-origin-when-cross-origin"
